@using LMSfinal.Models.EF
@model IEnumerable<ApplicationUser>
@{
    var classroom = ViewBag.Classroom as Classroom;
    DateTime selectedDate = ViewBag.SelectedDate;
    var existingIsPresent = ViewBag.ExistingIsPresent as Dictionary<string, bool> ?? new Dictionary<string, bool>();
    var existingIsLate = ViewBag.ExistingIsLate as Dictionary<string, bool> ?? new Dictionary<string, bool>();
    var existingNote = ViewBag.ExistingNote as Dictionary<string, string> ?? new Dictionary<string, string>();
}

@{
    ViewData["Title"] = "Attendance";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <h3>Điểm danh lớp: @classroom.NameClass</h3>

    <div class="card mb-4">
        <div class="card-body">
            <form method="get" class="row g-3 align-items-end">
                <input type="hidden" name="id" value="@classroom.Id" />
                <div class="col-md-4">
                    <label class="form-label">Chọn ngày điểm danh:</label>
                    <input type="date" name="date" class="form-control"
                           value="@selectedDate.ToString("yyyy-MM-dd")" onchange="this.form.submit()" />
                </div>
                <div class="col-md-4">
                    <a asp-action="AttendanceHistory" asp-route-id="@classroom.Id" class="btn btn-outline-info">
                        <i class="fa-solid fa-history"></i> Xem lịch sử
                    </a>
                </div>
            </form>
        </div>
    </div>

    <form asp-action="SaveAttendance" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" name="classroomId" value="@classroom.Id" />
        <input type="hidden" name="attendanceDate" value="@selectedDate.ToString("yyyy-MM-dd")" />

        <table class="table table-hover border">
            <thead>
                <tr>
                    <th>STT</th>
                    <th>Họ và Tên</th>
                    <th class="text-center">Hiện diện</th>
                    <th class="text-center">Trễ</th>
                    <th>Ghi chú</th>
                </tr>
            </thead>
            <tbody>
                @{
                    var students = Model.ToList();
                    TempData["Debug"] = $"Students: {students.Count}";
                    for (int i = 0; i < students.Count; i++)
                    {
                        var student = students[i];
                        bool isPresent = existingIsPresent.TryGetValue(student.Id, out var present) && present;
                        bool isLate = existingIsLate.TryGetValue(student.Id, out var late) && late;
                        string note = existingNote.TryGetValue(student.Id, out var noteValue) ? noteValue : string.Empty;

                        <tr>
                            <td>@(i + 1)</td>
                            <td>
                                @student.FullName
                                <input type="hidden" name="models[@i].StudentId" value="@student.Id" />
                            </td>
                            <td class="text-center">
                                <div class="form-check form-switch d-inline-block">
                                    <input type="checkbox" name="models[@i].IsPresent" value="true"
                                           class="form-check-input" @(isPresent ? "checked" : "") />
                                    <input type="hidden" name="models[@i].IsPresent" value="false" />
                                </div>
                            </td>
                            <td class="text-center">
                                <div class="form-check form-switch d-inline-block">
                                    <input type="checkbox" name="models[@i].IsLate" value="true"
                                           class="form-check-input" @(isLate ? "checked" : "") />
                                    <input type="hidden" name="models[@i].IsLate" value="false" />
                                </div>
                            </td>
                            <td>
                                <input type="text" name="models[@i].Note" class="form-control"
                                       value="@note" maxlength="500" placeholder="Ghi chú..." />
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <button type="submit" class="btn btn-success btn-lg">Lưu điểm danh</button>
    </form>
</div>

